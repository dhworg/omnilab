# host/overlay/

Overlay tree copied into the host image: `/etc` configs, branding,
default-user templates.

<!-- TODO Phase B.5 per project-spec-v1.md "First steps" — udev rules
(FTDI, CP210x, CH340, ST-Link, RP2040 BOOTSEL), group config (dialout,
plugdev, video, input on default user), `/etc/os-release` branding,
GRUB theme, wallpaper, RMW pinning + `ROS_DOMAIN_ID` in
`/etc/profile.d/`, `/etc/skel/` for default user. -->
