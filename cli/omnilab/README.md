# cli/omnilab/

Python package implementing the `omnilab` CLI: `new`, `up`, `down`, `sim`,
`perf-check`, `hw scan`, `hw flash`, `micro-ros`, `skill install`,
`freeze`, `doctor`.

<!-- TODO Phase B.4 per project-spec-v1.md "First steps" — Python CLI.
During dev lives in /var/home/parth/omnilab/ (mutable `/var`) for fast
iteration; baked into the host image at release time. -->
