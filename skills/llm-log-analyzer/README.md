# skills/llm-log-analyzer/

Optional skill-pack: Ollama + small model + service that watches Gazebo
logs and ROS topics, suggests parameter tweaks. Disabled by default
(RAM cost). Enabled via `omnilab skill install llm-log-analyzer`.

<!-- TODO Phase D.9 per project-spec-v1.md "First steps". Manifest +
install script + container layers. -->
