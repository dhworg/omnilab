# {{project_name}}

TurtleBot3 + nav2 starter, generated by `omnilab template install nav2-base`.

## Quick start

```sh
omnilab up                # launch the project container (Podman)
omnilab sim               # opens Gazebo + RViz with the demo world
```

Edit `omnilab.yaml` to change the Gazebo world, RMW, or GPU mode.
