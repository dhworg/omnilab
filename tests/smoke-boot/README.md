# tests/smoke-boot/

Smoke test 1: ISO installs in VM, host boots to XFCE, login works.

<!-- TODO Phase B.6 per project-spec-v1.md "First steps" + "Smoke tests"
#1. Runs on every ISO build in CI (smoke-tests.yml) and on first boot
via `omnilab doctor --full`. -->
