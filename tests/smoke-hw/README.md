# tests/smoke-hw/

Smoke test 5: `omnilab hw scan` detects each MCU
{Arduino Uno/Nano, ESP32, STM32 Blue Pill, RP2040}; `omnilab hw flash`
succeeds for each; micro-ROS topic appears in `ros2 topic list`.

<!-- TODO Phase B.6 + Phase C.8 per project-spec-v1.md "First steps" +
"Smoke tests" #5. Self-hosted runner (the dGPU machine with the four
MCUs plugged in); only runs on tags or label-triggered. -->
